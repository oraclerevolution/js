'use strict'

const add = tableau => {
  const compteur = 0
  const eltsTableau = 0
  const resultat = 0
  const inconnu = prompt("votre tableau contiendra combien d'elements ?")

  for (let i = 0; i <= inconnu; i++) {
    prompt[i]
  }
}
